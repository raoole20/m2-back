# =====================================================
# PostgreSQL
# =====================================================
POSTGRES_USER=m2_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=n8n_db
POSTGRES_PORT=5433

# Database names (used by init-db.sh)
N8N_DB=n8n_db
EVOLUTION_DB=evolution_db
API_DB=m2_api

# =====================================================
# API Database (Prisma connection string)
# =====================================================
# NOTE: This is constructed in docker-compose.yml, not here.
# For local development outside Docker, set it manually:
# DATABASE_URL=postgresql://m2_user:password@localhost:5433/m2_api?schema=public

# =====================================================
# Redis
# =====================================================
REDIS_HOST=localhost
REDIS_PORT=6379

# =====================================================
# Auth (JWT)
# =====================================================
JWT_SECRET=CHANGE_ME_GENERATE_SECRET
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_REFRESH_SECRET
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Dev/testing only. When "true", new registrations are auto-verified
# and login skips the EMAIL_NOT_VERIFIED check. Leave unset in production.
AUTH_BYPASS_EMAIL_VERIFICATION=false

# =====================================================
# Encryption (for channel credentials)
# =====================================================
ENCRYPTION_KEY=CHANGE_ME_32_BYTE_HEX_KEY

# =====================================================
# AI Providers
# =====================================================
OPENAI_API_KEY=sk-your-openai-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
GEMINI_API_KEY=your-gemini-api-key

# =====================================================
# Media Processor (multimodal: imagen/audio/video/documento)
# Defaults cuando AiContext no especifica modelo
# =====================================================
MEDIA_PROCESSOR_DEFAULT_MODEL_GEMINI=gemini-2.0-flash-exp
MEDIA_PROCESSOR_DEFAULT_MODEL_OPENAI=gpt-4o
MEDIA_PROCESSOR_DEFAULT_MODEL_ANTHROPIC=claude-3-5-sonnet-latest
MEDIA_MAX_FILE_SIZE_MB=20
MEDIA_DOWNLOAD_TIMEOUT_MS=15000

# =====================================================
# Storage (context files)
# =====================================================
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./uploads
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=

# =====================================================
# Meta (WhatsApp, Instagram, Messenger)
# =====================================================
META_APP_SECRET=your-meta-app-secret
META_VERIFY_TOKEN=your-verify-token

# =====================================================
# Telegram
# =====================================================
TELEGRAM_BOT_TOKEN=your-bot-token

# =====================================================
# App
# =====================================================
API_PORT=3000
NODE_ENV=development
SWAGGER_ENABLED=true

# =====================================================
# n8n
# =====================================================
N8N_PORT=5678

# =====================================================
# Evolution API
# =====================================================
EVOLUTION_PORT=8080
EVOLUTION_API_KEY=CHANGE_ME_GENERATE_API_KEY
EVOLUTION_API_URL=http://localhost:8080
